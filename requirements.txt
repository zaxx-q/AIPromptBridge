requests
flask
emoji>=2.8.0

# TextEditTool dependencies
pynput
pyperclip
darkdetect

# Modern GUI toolkit (optional for modern look)
customtkinter>=5.2.0

# Image handling for GUI (emojis, thumbnails, clipboard images)
Pillow

# Rich console output (colors, tables, etc.)
rich

# System tray (platform-specific backends)
infi.systray; sys_platform == "win32"
pystray; sys_platform == "linux"

# Audio loopback (Windows WASAPI via PyAudioWPatch; Linux uses stock PyAudio later)
PyAudioWPatch; sys_platform == "win32"

# PDF Tool dependencies
pypdf>=4.0.0