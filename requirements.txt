requests
flask
emoji>=2.8.0

# TextEditTool dependencies
pynput
pyperclip
darkdetect

# Modern GUI toolkit (optional for modern look)
customtkinter>=5.2.0

# Image handling for GUI (emojis, thumbnails, clipboard images)
Pillow

# Rich console output (colors, tables, etc.)
rich

# System tray (platform-specific backends)
infi.systray; sys_platform == "win32"
# Linux: pure D-Bus StatusNotifierItem (dms/waybar/niri). pystray alone falls back
# to XEmbed which cannot dock on pure Wayland without AppIndicator GI bindings.
jeepney; sys_platform == "linux"
pystray; sys_platform == "linux"

# Audio capture (Windows WASAPI loopback via PyAudioWPatch; Linux PortAudio/PipeWire via stock PyAudio)
# Linux system deps if wheel build fails: portaudio-devel (Fedora) or portaudio19-dev (Debian/Ubuntu)
PyAudioWPatch; sys_platform == "win32"
PyAudio; sys_platform == "linux"

# PDF Tool dependencies
pypdf>=4.0.0